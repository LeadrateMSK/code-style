# Стайл-гайд по Vue • Leadrate 🎨
Вся информация сформирована тут на основе:
- [оффициального стайл-гайда (рекоммендаций)](https://v2.vuejs.org/v2/style-guide/index.html);
- опыта работы с [Nuxt.js](https://nuxtjs.org/docs/get-started/directory-structure);
- статьи "[How to Structure a Large Scale Vue.js Application](https://vueschool.io/articles/vuejs-tutorials/how-to-structure-a-large-scale-vue-js-application/)";
- репозитория по код-стайлу "[Vue.js Component Style Guide](https://github.com/pablohpsilva/vuejs-component-style-guide "Vue.js Component Style Guide")"

Если вы используете Vue в проекте, это отличная возможность избежать ошибок и антипаттернов. Однако мы не считаем, что любое подобное руководство идеально подходит для всех команд или проектов, поэтому разумные отклонения поощряются на основе полученного опыта, окружающего технологического стека и личных ценностей.

## Содержимое
1. Модульная разработка
2. Именование компонентов
3. Структура каталогов
4. Структура компонента (+ composition API)
5. Именование методов и свойств компонентов
6. Не рекоммендуется или не использовать

## 1. Модульная разработка
Всегда старайтесь, чтобы ваше приложение состояло из небольших модулей, каждый из которых умеет выполнять только одну задачу.

Модуль по определению это небольшая ограниченная часть приложения. Самодостаточный функционально. Организация Vue позволяет создавать подобные модули ориентируясь на визуальные компоненты.

### Почему?
Модули небольших размеров легче взять для использования - понять что они делают, дорабатывать или переиспользовать. И вам и всей вашей команде.

### Как?
Старайтесь чтобы каждый Vue компонент соответствовал принципам [FIRST](https://addyosmani.com/first/):
- решающий одну задачу;
- независимый;
- переиспользуемый;
- небольшой;
- простой в тестировании.

## 2. Именование компонентов
В целом, имя каждого компонента должно соответствовать следующим критериям:
- **Понятное:** в меру детальным, в меру абстрактным;
- **Короткое:** состоит из 2-3 слов;
- **Произносимое:** чтобы его можно было упомянуть в обсуждении.

### Почему?
Имя компонента используется людьми. Имя должно облегчать коммуникацию, а также обеспечивать ускоренное ознакомление с кодом.

### Как?
```html
<!-- неправильно -->
<ui-slider></ui-slider> <!-- все компоненты - так или иначе UI элементы, приставка не нужна -->
<slider></slider> <!--не соответствует спецификации HTML5, а также в 1 слово -->

<!-- правильно -->
<AppHeader /> <!-- крупный компонент, включающий много дочерних компонентов -->
<UserList /> <!-- компонент с бизнес-логикой -->
<range-slider /> <!-- компонент элемент интерфейса -->
```

### Именование по типам компонентов
```html
<!-- крупные части приложения начинаем с App* -->
<AppHeader />
<AppMain />
<AppFormRequest />
<AppFeed />

<!-- компоненты с бизнес-логикой именуем по назначению -->
<UserList />
<CardsGrid />
<RssFeed>
	<!-- итерируем компоненты постов -->
</RssFeed>
<FormBuilder />

<!-- компоненты элементы интерфейса, если универсальные (u - universal) -->
<u-button></u-button>
<u-select />
<u-tabs />
<u-table />
<u-form>
	<!-- элементы формы -->
</u-form>
```

### Почему иногда camelCase, а иногда kebab-case?
Все теги в HTML5 относятся к построению интерфейса, и все они пишутся в стиле lowercase. Когда мы пишем в стиле kebab-case, мы избегаем конфликтов - используем lowercase, а также указываем, что это именно компонент Vue, а не тег HTML5.

## 3. Структура каталогов
Данная структура предполагается без использования Nuxt.js.
Для Nuxt.js можно перенять только структуру папок: **assets**, **components**, **pages**, **plugins**, **router**, **store**

- **assets**
-- **img**
-- **scss**
-- **fonts**
- **components**
-- **apps**
-- **universal**
---- index.js
- **pages**
- **plugins**
- **static**
- **router**
- **store**
- **test**
-- **__mocks__**
-- jest.setup.js
- App.vue
- main.js

### assets
В данной папке располагаются файлы для UI оформления: шрифты, стили, изображения.

### components
Папка для хранения всех компонентов с бизнес-логикой.

Папка *содержит подпапку* **apps**, где располагаются все `<App*/>` компоненты.

Папка *содержит подпапку* **universal** с файлом index.js, где как раз и регистрируем все `<u-*></u-*>` компоненты.

### pages⚠️
**Папка исключение!**
Папка для хранения всех страниц приложения. Именуем компоненты 1-2 словом в стиле lowercase, но свойство name компонента должно содержать 2-3 слова. 

Например:
```html
<!-- файл /pages/news/index.vue -->
<template>
	<div>News...</div>
</template>

<script>
	export default {
		name: 'NewsPage'
	}
</script>
```

### plugins
Папка для хранения глобальных плагинов приложения (с надстройками). Например:
- **plugins**
-- $axios.js
-- $moment.js
-- localize.js

Почему некоторые плагины начинаются со знака $? - потому что так мы показываем, что это надстроенные нами плагины для глобального использования. Как, например, настроенный инстанс axios для использования API на сервисе.

### static
Папка для хранения статичных файлов, которые, вряд ли будут когда-либо изменены. Например: favicon, manifest.json, robots.txt и т.п.

### router
Файлы с URL маршрутами сайта. Желательно зеркальное отражение структуры папки **pages**.

#### Рекоммендация к объекту router:
По возможности, подключайте все компоненты динамическими импортами:
```javascript
import { createRouter, createWebHistory } from 'vue-router';

const routes = [
    {
        path: '/',
        name: 'index',
        component: () => import('/pages/index'),
    },
    {
        path: '/news',
        name: 'news.index',
        component: () => import('/pages/news/index'),
    },
    {
        path: '/news/:slug',
        name: 'news.show',
        component: () => import('/pages/news/_show'),
    },
];

const router = createRouter({
    history: createWebHistory(),
    routes: routes
});

export default router;
```
Данная структура позволит разделить JS файл на чанки. Это нужно для оптимизации итогового приложения.

Принцип идентичен принципу работы в Nuxt.js.

### store
Папка с глобальным хранилищем (как правило, Vuex или Pinia).

### test
Папка со всеми тестами приложения (как правило, тестирование с jest).
**Все моковые данные** (демо-данные) также содержатся в этой папке (папка **\_\_mocks\_\_**), и могут быть использованы в любой части приложения на этапе разработки.

## 4. Структура компонента (+ composition API)

## 5. Именование методов и свойств компонентов

## 6. Не рекоммендуется или не использовать
